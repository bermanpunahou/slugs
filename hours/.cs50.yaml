check50:
  checks: "hours.c"
    demo: # defines a check called demo
      - run: ./hours # run program
        stdout: Hello, world!  # expect Hello, world! in stdout
        exit: 0 # expect program to exit with exitcode 0
    files:
    - !include "hours.c"
